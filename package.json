{
  "name": "boilerplate",
  "version": "1.0.0",
  "scripts": {
    "build": "cd back && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "cacheDirectories": [
    "front/node_modules"
  ],
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
} 